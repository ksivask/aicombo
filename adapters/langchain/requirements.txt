fastapi>=0.110.0
uvicorn[standard]>=0.27.0
httpx>=0.27.0
langchain>=0.2.0
langchain-openai>=0.1.0
# api=messages (claude native). 1.4.x pairs with langchain-core 1.2.x.
# ChatAnthropic uses `anthropic_api_url` (alias `base_url`) +
# `anthropic_api_key` (alias `api_key`). The underlying anthropic SDK
# http_client is injected via a cached_property override in
# framework_bridge._install_anthropic_hooked_clients so wire bytes
# flow into our httpx event hooks.
langchain-anthropic>=1.4.0
# MCP tool-calling: convert MCP tools → LangChain tools, bind to ChatOpenAI
# so the LLM can emit tool_calls that the adapter dispatches over MCP.
# Also pulls in `mcp` (the official Python SDK) which provides
# StreamableHttpConnection → ClientSession plumbing with httpx_client_factory
# support, so our wire-bytes capture hooks fire on every MCP call.
langchain-mcp-adapters>=0.2.0
pytest>=8.0.0
