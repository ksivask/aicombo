fastapi>=0.110.0
uvicorn[standard]>=0.27.0
httpx>=0.27.0
# crewai 1.14+ routes openai/anthropic/ollama providers to their native
# SDKs; we inject httpx.Client into the native clients for wire capture.
crewai>=1.14.0
# Native Anthropic SDK (required for api=messages via LLM(model="anthropic/...")).
anthropic>=0.40.0
# MCP: crewai has no langchain-mcp-adapters equivalent, so we wrap the
# fastmcp Client ourselves inside a crewai.tools.BaseTool subclass.
fastmcp>=2.0.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
